name: "CodeQL"
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    - cron: '40 23 * * 0'

jobs:
  analyze:
    uses: cyberuni/.github/.github/workflows/codeql-analysis.yml@v2
    permissions:
      actions: read
      contents: read
      security-events: write
